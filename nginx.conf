server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # Statische Assets werden direkt ausgeliefert
  location / {
    try_files $uri $uri/ /index.html;
  }

  # Gzip kann optional per image-weit aktivierter gzip.conf erfolgen; hier minimal gehalten
}
